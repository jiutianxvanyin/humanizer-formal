# Changelog

本项目的版本记录遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.0.0/) 格式。

版本号需同步两处：`.claude-plugin/plugin.json` 与
`.claude-plugin/marketplace.json`。`SKILL.md` 的 frontmatter 只有 name、
description 与 license 三个字段，不带版本号。

## [1.1.0] - 2026-09-15

### Added

- **A2 补入反向检查。** 改完要看有没有出现证据支撑不住的绝对化断言，
  需要时把限定语补回去。删限定语和加限定语是同一条规则的两面，本条不是
  单向削减。
- **A22 搭配错配。** 词都对、配对在中文里没有的那一类。两条判据：反查动词的
  常规宾语；以及「要解释这个搭配为什么成立，它就不成立」。这一类词表查不
  出来，只能逐句反查。三种形态中动宾错配与定中生造归 A 族，术语跨域挪用
  归 B 族。
- **B4 比喻的使用。** 说明性类比与修辞性比喻分开判定：前者保留，按局部约束
  （一个机制一个类比、同段不超过一处、相邻两段不连续），不设全文上限，
  该给不给同样算缺陷；后者一律改直陈，含暗喻、抽象操作物理化、拟人三种
  形态。另含方向性词语一节，判据是能不能指出那条轴。
- **B5 优先使用中文固有词。** 材料与用户都未指定术语时，选词优先用中文固有
  说法而非外来语直译。两条护栏：本领域中文文献通用的术语不算，没有自然
  替换就保留。
- **语域适用范围。** 输出一律按 L3 为准，写正文时按该文档的目标语域执行，
  未指明时取 L2。判据是文字的去向而非格式。L4 与 L5 在任何情况下禁止。
- **术语量按证据密度调。** 专业度有证据与术语两个来源，两者互补。有足够
  数字或出处支撑时降低术语量。在目标语域确定之后执行，不改变语域级别。
- 第三个可信度标签 `设计`，用于明确的设计决定而非测量结论。上述三条新规则
  全部标 `设计`。
- `references/patterns-zh.md` 新增假阳性防护一节，收录 11 条误判特征的逐条
  理由，原先只存在于 `SKILL.md` 的压缩版。

### Changed

- **正文默认语域由 L3 改为 L2**，语域标尺的「用在哪」一列重新划分：L2 收
  学位论文、研究报告、投资备忘录、尽调报告、行业分析、董事会材料；L3 收
  教科书、科普、面向非专业读者的说明与对外宣传。此改动同时消除了一处内部
  矛盾：`patterns-en.md` 原先把商务报告归入 L3（名词化 35–39），而
  `register-business.md` 要求参照 10-K（实测名词化 49.6–50.6），两个锚点
  互斥。
- **共现阈值收紧**：由「同一段出现三四种痕迹才是证据」改为「同一段出现两处
  以上不同痕迹即为修改信号」，并明确是不同痕迹共现而非同一痕迹反复出现。
- `SKILL.md` 中原标为「B3 目标语域对照」的一节改名为「按目标语域取值」。
  该节不是规则，占用 B3 编号会与 `patterns-zh.md` 的 B3（名词化后缀堆叠）
  冲突。`SKILL.md` 同时补入 B3 的简述。
- `patterns-zh.md` B3 的取值改为按证据密度确定，不再按文档类型固定参照年报。
- **`SKILL.md` 的 A 族编号改为与 `patterns-zh.md` 一致。** 两份文件此前从 A4
  起就对不上（`SKILL.md` 的 A5 是破折号，`patterns-zh.md` 的 A5 是「被」字
  滥用），变更清单只要注明编号就会指错条目。`SKILL.md` 只收最高频条目，
  编号因此不连续；两条英文条目改用名称，不占中文侧编号。
- **D1 改名**，由「不要磨掉四字格与排比」改为「不要拆散四字格与排比」。原名
  本身是 A22 所说的动宾错配，磨的常规宾语是刀、墨、豆子。该坏搭配已连带
  复制到 `SKILL.md`、`README.md`、`CHANGELOG.md` 与 `RESEARCH.md` 共五处，
  全部改正。
- **D2 缩小适用范围**，只保护材料里与用户指定的术语。改写中自己新造的说法
  不受保护，要过 A22，不能拿 D2 当坏搭配的免检理由。
- **B5 的判据改为不依赖词表**：判断标准是「这个词是不是某个英文词的直译，
  而中文里本来另有说法」，对照表降为示例。
- **加入「词表只是示例」的总则**，写进 `SKILL.md` 第二步开头、「绝不做的事」
  与审校流程第 2 步：坏搭配与直译词都列不完，判断必须逐句做，不能拿词表当
  检查清单。审校流程第 2 步明确拆成两种检查，词表提示的与词表查不到的。
- 全仓库按 A22 逐句反查，改正 34 处搭配错配，其中 26 处来自本轮之前的文本，
  8 处是本轮新写条文时产生的。

- `SKILL.md` 压缩：词层面的改写示例由「改前 / 改后」两段式改为单行箭头式，
  实测数字移入 `references`，主文件只保留可信度标签，假阳性清单压缩为关键词
  行并将逐条理由移入 `patterns-zh.md`。加入四条新规则后主文件净增约 3%。

### Changed（1.0.0 发布后、1.1.0 之前的改动）

- 项目更名为 `humanizer-formal`。仓库名、Skill 名称、
  `.claude-plugin/plugin.json`、`.claude-plugin/marketplace.json` 及文档中的
  相关引用同步更新。
- 补回构建时丢失的 `.claude-plugin/plugin.json` 与 `marketplace.json`。
- `SKILL.md` 的 `description` 改为中英双语。原版为纯中文，用户以英文提出
  请求时触发准确度偏低。
- `plugin.json` 与 `marketplace.json` 补入 `homepage`、`repository`、
  `author.url` 与插件 `version` 字段。

### Added

- 新增 Issue 模板：误判反馈（`false-positive.yml`）、语料贡献
  （`corpus-contribution.yml`）。

## [1.0.0]

首个正式版本，发布于更名前的项目名下。从 `Humanizer-zh` 衍生，
面向商务与学术书面语重做。

### Added

- **两族判定架构。** A 族是机器痕迹，一律改；B 族是语域标记，按目标语域
  调整。实测发现机器翻译的中文抽象名词密度是全部语料中最低的
  （0.12–0.50‰），人写咨询研究报告最高（7.12‰），相差 14 到 59 倍。
  所以 buzzword 多寡标记的是语域，不是作者身份，两者混用必然误判。
- **五级语域标尺。** 默认目标为 L3（本科教科书用语），明令禁止降到
  L4 口语层。降级降的是句法复杂度与抽象名词密度，不是降正式度。
- **假阳性防护清单与「共现才算证据」原则。** 几乎每个指标的人写区间与
  机器区间都相邻或重叠，单个痕迹不构成判定依据。
- **要保留的人写痕迹清单**，以及三种输出形态与改写后的两项自查。
- 中文侧 34 条规则、英文侧专属条目与对照表。每条 A 族规则标注可信度：
  `实测` 表示本项目语料验证过，`沿用` 表示继承上游、未经验证。
- `RESEARCH.md`，记录语料构成、量化方法、研究过程与已知局限。

### Removed

- 上游的「注入个性」整节。第一人称抒情、幽默、锋芒在商务学术语域中会
  损害可信度，替换为具体性、立场承担、信息密度、可核查性四条。

### Changed

- **中文三字格与四字格排比由「AI 痕迹」反转为禁止改写。** 实测人写中文
  使用频率是机器翻译的 2 到 10 倍，跨四种语域一致。排比是汉语的本土
  节奏手段，不做这次检验会把好中文改坏。
- 三条上游规则经实测否定，已在 `references/patterns-en.md` 中单独列出：
  英文破折号频率不携带信号（三本同类人写教科书实测 0.00、0.98、2.34）；
  英文被动语态无区分度，区间完全重叠；英文名词化是语域标记而非机器痕迹
  （两家不同行业的 10-K 分别为 49.6 和 50.6）。
- 补收上游内容。所依据的中译本只收录了上游 35 条规则中的约 24 条，
  本版直接比对上游仓库补收九条并改写为中文语境版本，另补入英文连字符
  词对位置规则。

### 证据基础

约 56 万词英文、184 万字中文公开语料，覆盖七种中文语域与五种英文语域，
包括本科教科书、研究生专著、政府白皮书、企业年报、央行监管报告、
咨询研究报告、国际组织文件、专业人工翻译与机器翻译。

研究过程中有三个结论被数据推翻，全部记录在 `RESEARCH.md`：中文四字格
排比的方向判断反了、句长变异系数被样本量混淆、连接词变化率的方向搞错了。
